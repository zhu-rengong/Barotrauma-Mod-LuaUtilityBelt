<Project Sdk="Microsoft.NET.Sdk">
  <Import Project="..\SharedProject\SharedProject.projitems" Label="Shared" />
  <Import Project="../UserBuildData.props" />
  <Import Project="../Build.props" />

  <ItemGroup>
    <Reference Include="DedicatedServer">
      <HintPath>$(RefsPath)\OSX\DedicatedServer.dll</HintPath>
    </Reference>
    <Reference Include="BarotraumaCore">
      <HintPath>$(RefsPath)\OSX\BarotraumaCore.dll</HintPath>
    </Reference>
    <Reference Include="MonoGame.Framework.MacOS.NetStandard">
      <HintPath>$(RefsPath)\MonoGame.Framework.MacOS.NetStandard.dll</HintPath>
    </Reference>
  </ItemGroup>
  
  <PropertyGroup Condition=" '$(Configuration)' == 'Debug' ">
    <DefineConstants>TRACE;OSX;SERVER</DefineConstants>
  </PropertyGroup>

  <PropertyGroup Condition=" '$(Configuration)' == 'Release' ">
    <DefineConstants>TRACE;OSX;SERVER</DefineConstants>
  </PropertyGroup>

  <Target Name="CopyFiles" AfterTargets="Build">
    <ItemGroup>
      <Assemblies Include="$(TargetDir)$(TargetName).dll" />
      <DepsFiles Include="$(TargetDir)$(TargetName).deps.json" />
    </ItemGroup>
    <Copy SourceFiles="@(Assemblies)" DestinationFiles="$(MSBuildThisFileDirectory)..\ContentPackageBuilder\Content\bin\Server\OSX\$(TargetName).dll" />
    <Copy SourceFiles="@(DepsFiles)" DestinationFiles="$(MSBuildThisFileDirectory)..\ContentPackageBuilder\Content\bin\Server\OSX\$(TargetName).deps.json" />
  </Target>

</Project>